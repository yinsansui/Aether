# Aether 部署配置 - 使用预构建镜像
# 使用方法: docker compose up -d

services:
  postgres:
    image: postgres:15
    container_name: aether-postgres
    command: postgres -c max_connections=${PG_MAX_CONNECTIONS:-140}
    environment:
      POSTGRES_DB: aether
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      TZ: Asia/Shanghai
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres" ]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: aether-redis
    command: redis-server --appendonly no --save "" --requirepass ${REDIS_PASSWORD}
    healthcheck:
      test: [ "CMD", "redis-cli", "--raw", "incr", "ping" ]
      interval: 5s
      timeout: 3s
      retries: 5
    restart: unless-stopped

  app:
    image: ${APP_IMAGE:-ghcr.io/fawney19/aether:latest}
    container_name: aether-app
    env_file:
      - .env
    environment:
      # 需要组合的变量
      DATABASE_URL: postgresql://postgres:${DB_PASSWORD}@postgres:5432/aether
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379/0
      # Supervisor 需要的变量
      GUNICORN_WORKERS: ${GUNICORN_WORKERS:-2}
      # PostgreSQL 连接池配置
      PG_MAX_CONNECTIONS: ${PG_MAX_CONNECTIONS:-140}
      PG_RESERVED_CONNECTIONS: ${PG_RESERVED_CONNECTIONS:-10}
      DB_POOL_SIZE: ${DB_POOL_SIZE:-15}
      DB_MAX_OVERFLOW: ${DB_MAX_OVERFLOW:-15}
      # 容器级别设置
      TZ: Asia/Shanghai
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    ports:
      - "${APP_PORT:-8084}:80"
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped

volumes:
  postgres_data:


